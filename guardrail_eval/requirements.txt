torch
transformers
pandas
tqdm
# audit pipeline (run_audit_pipeline.py):
#   investigator = DeepSeek via NVIDIA endpoint (OpenAI SDK); judge = Groq (gpt-oss-120b)
openai
groq
python-dotenv
# XAI report (make_report.py): static charts + PDF assembly, no external
# binary (e.g. pandoc) required.
matplotlib
reportlab
# local_agent.py (--investigator-backend/--judge-backend local, RunPod only):
# investigator + judge as open-weight models loaded via `transformers`
# directly, no vLLM. Not installed here on purpose -- the default
# investigator (Qwen2.5-14B-Instruct-AWQ) needs an AWQ backend, a CUDA-only
# package that would break `pip install -r requirements.txt` on this
# CPU/Windows dev venv. Install it on the pod only, when using the local
# backend. Which package depends on the pod's transformers version --
# `transformers`'s own error message says which one it wants:
#   pip install gptqmodel   # newer transformers (confirmed working on the
#                            # 2026-07 RunPod pod, torch 2.11+cu128)
#   pip install autoawq     # older transformers versions may want this instead
# jlens (the interpretability package at the repo root) is used by
# run_guardrail_jlens.py / jlens_readout.py. Install it editable into this venv:
#   .venv\Scripts\python -m pip install -e ..
# It also pulls numpy + huggingface_hub. Not pinned here because it lives in
# this same repo and is installed from source, not from PyPI.
